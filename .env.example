# App
APP_PORT=8080
APP_ENV=local
DB_PATH=data/youtube_scripts.db

# LLM provider: "azure", "kiro", "openai", or "openrouter"
LLM_PROVIDER=azure

# Azure OpenAI / OpenAI-compatible endpoint
# app/llm.py uses AsyncOpenAI with base_url, so set this to the chat completions compatible base URL.
AZURE_API_KEY=your-azure-or-openai-compatible-api-key
AZURE_ENDPOINT=https://your-resource.openai.azure.com/openai/v1/
# For Azure AI Foundry projects, use:
# AZURE_ENDPOINT=https://your-project.services.ai.azure.com/models
AZURE_MODEL=gpt-5.4-nano

# Kiro Gateway
# For local Windows execution, localhost is usually correct.
# For Docker accessing a gateway on the host, use http://host.docker.internal:4000/v1.
KIRO_BASE_URL=http://localhost:4000/v1
KIRO_API_KEY=kiro-local
KIRO_MODEL=claude-haiku-4-5

# OpenAI
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-5.4-mini

# OpenRouter
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1
OPENROUTER_API_KEY=your-openrouter-api-key
OPENROUTER_MODEL=openai/gpt-5.4-mini
